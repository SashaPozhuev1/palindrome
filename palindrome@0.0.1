#include <iostream>
#include <string>
#include <cstring>

using namespace std;

int main()
{
	string s;
	getline(cin, s);
	int a = s.size(), b = 0;

	for (int i = 0; i < (a / 2); i++) {
			 if (s[i] == s[a-1-i]) b += 1;
	}
				 if (b == (a / 2)) cout << "1" << endl;
				 else cout << "0" << endl;
	//system("pause");
	cin.get();
	return 0;
}
